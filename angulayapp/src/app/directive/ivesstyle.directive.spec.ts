import { IvesstyleDirective } from './ivesstyle.directive';

describe('IvesstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new IvesstyleDirective();
    expect(directive).toBeTruthy();
  });
});
